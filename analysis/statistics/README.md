# Statistical Analysis

Scripts and pre-computed statistical outputs that support the paper's
claims. Each script should read from `analysis/csv/` and emit:

- A CSV summary into this directory.
- A figure into `analysis/figures/`.