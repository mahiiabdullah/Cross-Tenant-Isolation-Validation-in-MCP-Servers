# Experiment Scripts

Driver scripts that load a config from `configs/` and invoke the framework.

Suggested entry points:

- `run_experiment.py` — single-experiment runner.
- `run_matrix.py` — sweep over an experiment matrix.
- `verify_artifact.py` — sanity checks for reproducibility.