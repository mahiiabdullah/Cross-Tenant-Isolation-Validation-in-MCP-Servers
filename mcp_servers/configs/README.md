# MCP Server Configs

YAML / JSON configurations consumed by the framework to spin up
`vulnerable` and `secure` reference servers.

Suggested layout:

```
vulnerable.yaml
secure.yaml
multi_tenant.yaml
```